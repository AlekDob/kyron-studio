---
type: diary
project: kyron-studio
date: 2026-05-27
---

- [afternoon] (Alek) **WS-03 Phase 6 — DOM context + select/browse toggle**. (1) Toggle select/browse: nuovo state `selectMode` in `PreviewWorkspace`, invio `kyron-rev:mode` via postMessage all'iframe, cms `ReviewOverlay` ascolta e togla `data-kyron-rev-mode` con valore `armed` — in browse mode gli outline di selezione sono disabilitati. Componente `ModeToggle.tsx` (NEW) con icona cursore (select) / freccia (browse). (2) DOM context strutturato: estratti helper selezione in `review-selection.ts` (NEW, cms-side) con `extractSectionContext()` — risale al parent `<section>`, costruisce outline tree tag+classi depth 3 + lista immagini src+alt dalla sezione. Il contesto finisce nel payload `kyron-rev:select`. Studio propaga `SectionContext` nel `pendingTarget`. Studio-server: `formatSectionContext()` in `agent.ts` formatta outline + images nel preamble agente. (3) Drawer polish desktop: `AnnotationsDrawer` portato a 540px (era 480), aggiunto inset-4 + rounded-2xl + border + padding generoso. `AnnotationDetail` stessa larghezza 540px per coprire il parent. (4) Dev cookie middleware: `src/middleware.ts` (NEW) Edge-compatible (Web Crypto API, non node:crypto) che setta automaticamente cookie `kyron-rev` in dev quando `STUDIO_DEV_USER` e' configurato — elimina la necessita' di firmare manualmente il cookie durante lo sviluppo. KEY INSIGHT: il DOM context strutturato (outline + images) permette all'agente di proporre `restructure` e `replace-image` con cognizione di causa — sa quante colonne ha la sezione, quali immagini contiene, e come sono annidati gli elementi. Il toggle select/browse risolve il conflitto UX fra navigazione e selezione: prima l'utente era sempre in "selection mode" e ogni click era intercettato dall'overlay.
- [11:00] (Alek) **WS-04 Saleor live + deploy**. Integrazione catalogo prodotti reale da Saleor GraphQL in studio-server (`core/saleor/client.ts` — query pubblica channel-aware). ProductPicker e BundleBuilder ora caricano prodotti live con thumbnail. Committato e pushato su GitHub (`AlekDob/kyron-studio-server`, `AlekDob/kyron-studio`). Deploy triggerato via Coolify API. Documentata procedura deploy push-to-deploy in CLAUDE.md studio e inbox deploy note.
- [12:00] (Alek) **WS-04 Phase 3 — Modulo Portali + fix schema**. (1) Fix Zod schema: aggiunto `kind: "variant"` al discriminator `componentSchema.selection` — l'agente mandava `"variant"` ma lo schema accettava solo `"fixed"` e `"by-attribute"`. (2) Rinomina sidebar: "Onboarding scuole" → "Portali" (modulo unificato, rimosso duplicato "Portali scuole" coming-soon). (3) Dashboard `/portals`: server component con griglia card (nome, slug, citta'+provincia, stato Pill, prodotti, kit, data raccolta, origine agente/manuale) + empty state con CTA "Nuovo portale". Dati letti da studio-server `GET /api/v1/portals` che parsa i `.md` dal filesystem. (4) Chat onboarding spostata a `/portals/new`. (5) Feature 007-portals-module creata. Deploy su Coolify completato. KEY INSIGHT: leggere i portali dal filesystem (.md con frontmatter YAML) e' piu' affidabile di Payload (schema drift) e il formato e' lo stesso che serve al downstream seed — zero impedance mismatch.
- [16:00] (Alek) **Responsive mobile completo**. (1) Shell DesktopShell reso responsive: sidebar nascosta sotto `lg`, drawer overlay 280px con hamburger nella top bar sticky. Viewport export con `userScalable: false` e `maximumScale: 1` per bloccare zoom su mobile. Usato `100dvh` per gestire barra browser dinamica. Drawer si chiude automaticamente al cambio route. (2) `MobileChatOverlay` (NEW) — componente shared FAB + fullscreen overlay per pannelli chat/context nascosti su mobile. Icona configurabile. (3) Integrato in DataWorkspace (Editor Dati), PreviewWorkspace (Review Editor con `ReviewPanel` estratto), PortalsWorkspace (lista/detail portali con icona LayoutList). (4) SettingsLayout: tab rail orizzontale scrollabile su mobile, verticale su desktop. Padding responsive. (5) Fix review: FAB nascosto quando overlay aperto (z-index conflict), ReviewPanel wrappato in div flex-col (layout overlay corretto). Feature 001 aggiornata.
- [evening] (Alek) **WS-03 Phase 7 — Conferma sync + annotazione manuale**. (1) Conferma proposta sync: `confirmProposal`/`cancelProposal` in `PreviewChat` non chiamano piu' `runStream` dopo la conferma. Prima: aggiungevano l'annotazione localmente e mandavano un user message sintetico ("Confermo, aggiungi al bundle.") all'agente che ri-elaborava tutta la conversazione solo per dire "ok" — "Ragionamento..." girava a vuoto a ogni click. Ora: sync, aggiunta locale + ack assistente locale ("Aggiunta al bundle."). L'agente riceve il nuovo `annotationsCount` nel context al prossimo messaggio utente. (2) Annotazione manuale: nuovo `ManualAnnotationForm.tsx` accessibile via bottone "Aggiungi manualmente" sulla `SelectionChip` (prop opzionale `onManual`). Form inline con selettore `kind` (default derivato da `nodeKind`), textarea testo proposto, input hint nuova immagine (per replace-image), textarea nota libera. Submit costruisce `ProposeArgs` dal `pendingTarget` + form, riusa `buildAnnotation()` per produrre `Annotation` completa, chiama `onAdd`, deseleziona. Zero chiamate all'agente. KEY INSIGHT: il round-trip "Confermo → agente risponde" sembrava innocuo ma costava 3-5 secondi per ogni conferma e dava la sensazione che l'agente stesse facendo qualcosa di sbagliato. La conferma e' un'azione client-side al 100% (l'annotazione e' gia' stata costruita lato agente), bypassare il modello non perde nulla. L'annotazione manuale e' un fallback fondamentale: quando l'utente sa esattamente cosa scrivere, l'agente e' overhead.
- [evening] (Alek) **Fix bug loop multi-kit BundleBuilder**. Quando l'utente, dopo aver submittato il primo kit, rispondeva "si" a "Vuoi aggiungere un altro kit?", l'agente emetteva solo testo ("Per favore, crea un altro kit/bundle utilizzando il builder visuale.") SENZA richiamare il tool `render_bundle_builder`. L'utente vedeva un'istruzione orfana che sembrava puntare al builder gia' submitted in stato read-only sopra. Fix in `studio-server/src/features/onboard-school/prompt.ts` step 7: riscritto come loop esplicito (a-f) con regola ferrea "se devi mostrare un builder, DEVI chiamare il tool — mai solo testo" e indicazione che i builder precedenti sono read-only e non riusabili. Aggiornata feature 006 con sezione "Loop multi-kit". KEY INSIGHT: per loop di tool generative-UI il prompt deve essere chirurgico — l'LLM tende a "rispondere conversazionalmente" invece di reinvocare il tool, e basta un'ambiguita' ("per OGNI kit chiama...") per farlo deragliare al secondo giro.
- [evening] (Alek) **Modulo Dati — rendering form piu' leggibile**. Ripulita la sezione `/dati`: card header e liste record piu' scansionabili, titoli localizzati anche quando arrivano come JSON stringificato, ricerca con icona e stati vuoti piu' chiari. Nel detail `/dati/[slug]/[id]` eliminata la griglia IT/EN a due colonne: i campi localizzati sono ora stacked, i nomi campo sono umanizzati (`productType` → `Product type`), le relazioni senza label sono collassate in riepiloghi espandibili invece di pareti di ID. Aggiunto rendering `localized-rich` read-only per rich text Payload: mostra anteprima testuale pulita + JSON ispezionabile, senza inviare `[object Object]` nel submit. KEY INSIGHT: il form generico deve distinguere campi editabili da contenuti strutturati; trattare ogni oggetto come input testuale corrompe i dati.
- [evening] (Alek) **Modulo Dati — editor relazioni catalogo**. Aggiunto editing relazioni per i target sicuri del catalogo prodotto: `brand → brands` e `categories → product-categories`. `brand` ora usa una select con opzione "Nessuna relazione"; `categories` usa checkbox per mantenere/rimuovere le relazioni correnti e un blocco espandibile per aggiungerne piu' di una prima del salvataggio. Le relazioni senza target noto restano read-only per evitare PATCH sbagliate. Estratti helper field detection in `src/lib/data-fields.ts` e UI relazioni in `RelationFieldEditor.tsx`, riportando il page detail sotto le 300 righe. KEY INSIGHT: meglio una mappa esplicita dei target editabili che tentare inferenza generica da nomi campo Payload.
- [evening] (Alek) **Modulo Dati — array strutturati e dropdown puliti**. Corretto il detection bug che trattava `features` e `specs` come relazioni solo perche' le righe Payload hanno un `id`: ora diventano editor array inline con campi localizzati IT/EN, rimozione per riga e pannello "Aggiungi elemento". Aggiunti componenti piccoli e DRY `DataSelect` e `DataDisclosure` con icona Lucide, `appearance-none` e marker nativo nascosto, usati nel detail dati per select relazioni, boolean e disclosure. Il salvataggio server ricostruisce gli array strutturati da FormData preservando gli `id` esistenti e ignorando la nuova riga vuota. KEY INSIGHT: gli array Payload e le relationship hasMany hanno shape simili in risposta (`id`), quindi la mappa dei target relazione deve guidare la classificazione, non la sola presenza di `id`.

- [notte] (Alek) **WS-04 Phase 5 — detail panel ricco/animato + editing inline + CRUD portale completo**. Side panel: layout flex con aside animata 420px→560px (transition-[width] 500ms ease-out), fadeIn 280ms sul contenuto al cambio portale. `PortalDetail` completo al posto della vecchia `DetailView` povera (stato pill, indirizzo, spedizione, catalogo pill chips, kit con componenti). Editing inline: `InlineText` (click→input→Enter/✓, Esc annulla), `InlinePrice` (parsing virgola/punto), `CatalogEditor` (chip × rimovibili, dropdown Saleor lazy-loaded per aggiungere), `BundleCard` (nome/prezzo inline, aggiungi/rimuovi componenti, cestino doppio-click conferma). `handleRefreshDetail` in `PortalsWorkspace` refetcha portale+lista dopo ogni mutation. Backend: `resolvePortal(query)` fuzzy match (slug o nome case-insensitive), tool `add_bundle_to_portal`/`update_catalog`/`update_bundle`/`remove_bundle`. Studio-server `4a70e5d`, studio `b54febc`. Feature 007 aggiornata, workstream 04 status → phase-5-done.
- [notte] (Alek) **Mobile responsive polish — input fixed, FAB posizioni, Comandi nascosto**. (1) PortalsWorkspace: struttura scroll corretta — chat in wrapper `overflow-y-auto` separato dal form (che resta in flex a fine colonna). (2) MobileChatOverlay: React portal, posizioni `top-right`/`bottom-right` configurabili, FAB fisso con safe-area iOS. (3) Posizioni: Dati/Anteprima = bottom-right (chat), Portali = top-right (side panel sinistro occupa lo spazio). (4) Comandi ⌘K nascosto su mobile (`hidden lg:flex`). Feature 001 aggiornata.
