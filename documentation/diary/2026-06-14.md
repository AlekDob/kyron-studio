---
type: diary
project: studio
date: 2026-06-14
---
- [19:00] (Alek) Feature 010 v4 — **cambio stato lavorazione** nel drawer (bottoni segmentati: Nuovo/In preparazione/Spedito/Consegnato/Annullato) via server action `updateOrderStatusAction` → PATCH BFF, update ottimistico + feedback ("email inviata" se spedito). Badge stato lavorazione anche in lista (+ pagamento). Ricerca: chiarito che lo Stripe cercabile è il **PaymentIntent** (`pi_`, quello che apre "Apri su Stripe"), non il PaymentMethod (`pm_`, che Saleor non salva). Ordini di **test esclusi** lato BFF. Email "spedito" ai clienti reali OFF (allowlist prod = gmail@alekdob.com).
- [18:00] (Alek) Feature 010 v3 — drawer ordine spostato a **destra** su desktop (era a sinistra) e ora **alto quasi tutto lo schermo** con padding-y (fix: `lg:bottom-auto` accorciava il pannello). Aggiunta sezione **Dati fiscali** nel drawer (codice fiscale, P.IVA, SDI, azienda — solo se presenti); CF/P.IVA/SDI inclusi anche nella ricerca. Backend feature 008 esteso: i campi fiscali arrivano da `billingAddress.metadata` Saleor (chiavi `fiscalCode`/`vatNumber`/`sdiCode`, popolate dal checkout storefront — Brain ecommerce `fiscal-data-checkout`).
- [17:00] (Alek) Feature 010 v2 — UX ordini rifatta: lista **ordine desc** + **raggruppata per giorno** (Oggi/Ieri/data, fuso Europe/Rome), **ricerca** (n° ordine, cliente nome/email/telefono, transazione Stripe), **drawer** dettaglio (scivola da **sinistra** desktop, **bottom sheet** mobile, pattern AnnotationsDrawer). Drawer mostra **dati cliente** (nome/email/telefono/indirizzo) e **link diretto a Stripe** (pspReference → dashboard.stripe.com/payments, nuova scheda). Rimossi `OrdersTable`/`OrderCard` (tabella+card espandibili) → `OrdersList`+`OrderListRow`+`OrderDrawer` (più responsive). Backend feature 008 esteso: customerName/phone/address + pspReference + stripeUrl (da billingAddress + transactions Saleor). typecheck + build verdi.
- [15:45] (Alek) Feature 010 — nuovo **modulo Ordini** (`/orders`) per i commerciali: lista tutti gli ordini dei portali, filtrabile per **data** (da/a, nei searchParams → refetch), **portale** e **agente** (filtro client-side sul payload, zero refetch, pattern Analytics). Per ordine: portale (link allo shop), agente, codice meccanografico, cliente, totale, **stato** pagamento+evasione (Pill), righe prodotto espandibili (cod+descr×qty+€). KPI n.ordini + totale. Tabella desktop / card mobile (`lg:hidden`). Accesso a tutti gli utenti Studio (admin+editor), read-only. Backend: studio-server feature 008 `GET /api/v1/orders`. Riuso UI `Card/Select/Input/Pill`, niente emoji, file <300 righe. typecheck + `next build` verdi (`/orders` ƒ dynamic). KEY: la verifica con dati reali è post-deploy (il `SALEOR_APP_TOKEN` è solo-prod, già presente da feature 007).
