---
type: diary
project: studio
date: 2026-06-14
---
- [15:45] (Alek) Feature 010 — nuovo **modulo Ordini** (`/orders`) per i commerciali: lista tutti gli ordini dei portali, filtrabile per **data** (da/a, nei searchParams → refetch), **portale** e **agente** (filtro client-side sul payload, zero refetch, pattern Analytics). Per ordine: portale (link allo shop), agente, codice meccanografico, cliente, totale, **stato** pagamento+evasione (Pill), righe prodotto espandibili (cod+descr×qty+€). KPI n.ordini + totale. Tabella desktop / card mobile (`lg:hidden`). Accesso a tutti gli utenti Studio (admin+editor), read-only. Backend: studio-server feature 008 `GET /api/v1/orders`. Riuso UI `Card/Select/Input/Pill`, niente emoji, file <300 righe. typecheck + `next build` verdi (`/orders` ƒ dynamic). KEY: la verifica con dati reali è post-deploy (il `SALEOR_APP_TOKEN` è solo-prod, già presente da feature 007).
