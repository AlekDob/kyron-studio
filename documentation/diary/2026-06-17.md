---
type: diary
project: studio
date: 2026-06-17
---
- [13:30] (Alek) Modulo Ordini: 2 aggiunte. (1) Drawer mostra **Studente** + **Classe** (da `billingAddress.metadata` studentName/studentClass, ecommerce feature 028) sotto Cliente — studio-server li mappa già perché la query legge billingAddress.metadata. (2) Azione **"Bonifico pagato"** per ordini `paymentMethod=bank-transfer`: nuovo `markOrderAsPaid` (mutation Saleor `orderMarkAsPaid`, transactionReference) in `core/saleor/orders.ts` + handler `features/orders/bank-transfer.ts` (mirror di teacher-card: orderMarkAsPaid → paymentStatus FULLY_CHARGED, metadata `bankTransferPaidAt` best-effort, email "bonifico ricevuto" al cliente) + endpoint `POST /api/v1/orders/bank-transfer-paid`. Frontend: `BankTransferBlock` nel drawer, `markBankTransferPaidAction`, override ottimistico `paid` in OrdersView (flippa paymentStatus), badge "Bonifico da incassare". Scelta Opzione B (orderMarkAsPaid) vs solo-metadata: così badge/Danea/report restano coerenti. tsc pulito su studio + studio-server. Cross: [[ecommerce feature 028]], decision-019.
- [14:05] (Alek) **Deployato in prod**: studio (`758cd92`) + studio-server (`666d9c3`) via Coolify (uuid `qp5tw0o90drest5wnnof8647` + `x5bzjhuxbl4ab4j5tnkbckq0`). Verificata la strategia mark-as-paid dei channel prod = **`payment_flow`** (non TRANSACTION_FLOW come temevo): `orderMarkAsPaid` crea un Payment e porta `paymentStatus` a FULLY_CHARGED, pulito per i bonifici (nessun pagamento preesistente). Studio up (200), studio-server raggiunto internamente dal frontend (no FQDN pubblico). Annuncio team dei 2 metodi inviato via Resend (Panzerottino). Verifica funzionale finale (premere il bottone su un ordine bonifico reale) lasciata ad Alek.
