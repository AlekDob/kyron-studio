---
type: diary
project: studio
date: 2026-06-04
---
- [ProductPicker] (Alek) Aggiunto "Seleziona tutti / Deseleziona tutti" (sui prodotti filtrati) e sconto per-prodotto (flessibile: % o prezzo finale EUR) nel picker dell'onboarding. Riga prodotto estratta in `ProductPickerRow.tsx` (file < 300 righe). Submission ora `{ selectedSlugs, productDiscounts: [{slug, kind, value}] }`. Registry mappa `productDiscounts`→`initialDiscounts` per il render readonly dei turn vecchi. Draft (`PortalDraft.productDiscounts`) cattura gli sconti. Solo registrato: applicazione Saleor delegata all'onboarding ecommerce. tsc verde.
- [ProductPicker/LivePortalCard] (Alek) Sconto: la riga del picker ora mostra il prezzo finale netto (originale barrato + netto in accent) calcolato live (`netPrice`: percent = -%, eur = prezzo finale). Il riepilogo CATALOGO della LivePortalCard mostra l'etichetta sconto sul pill (accent) dei prodotti scontati.
- [ProductPicker] (Alek) Fix bug selezione singola: la checkbox a destra era diventata uno <span> non cliccabile (solo il nome a sinistra toggolava) → ora e' un <button> che chiama onToggle. Aggiunta riga istruzioni sconto (%=sconto, €=prezzo finale) sotto la ricerca.
- [PortalsChat/PortalDetail] (Alek) Fix 2 bug. (1) Logo uploader spariva: un turno assistant teneva una sola card (pendingUi singolo) e il picker sovrascriveva il logo quando l'agente li emetteva insieme. Ora ChatTurn.uis e' una lista: piu' card per turno restano tutte; submit keyed (turnIdx, cardIdx). (2) Sconti non visibili nel dettaglio portale: aggiunto productDiscounts al tipo PortalDetail.catalog (gateway) e reso l'etichetta sconto sui pill del CatalogEditor (riuso formatDiscount), come LivePortalCard.
- [PortalsList] (Alek) Gestione manuale portali dalla lista: selettore stato per riga (draft/review/approved/onboarded → PUT /api/portals/{slug}) e cestino con conferma a due click (DELETE). Aggiunti gateway.deletePortal + handler DELETE in app/api/portals/[slug]/route.ts (backend studio-server gia' pronto: PUT/:slug con status + DELETE/:slug). PortalsWorkspace passa onChangeStatus/onDeletePortal e refresha lista/detail.
- [PortalsList] (Alek) Lista portali a 2 stati + grouping: selettore ridotto a Bozza (draft) / Live (onboarded); stati legacy (review/approved) normalizzati a Live via normStatus(). Raggruppamento con intestazioni: gruppo "Bozze" in cima, "Live" sotto (PortalGroup, render nascosto se vuoto). Riga estratta in renderRow.
- [LogoUploader] (Alek) Fix "Errore di rete" upload logo: il componente postava DIRETTAMENTE a studio-server (NEXT_PUBLIC_STUDIO_SERVER_URL ?? http://localhost:8790), irraggiungibile dal browser in prod (+mixed-content). Ora passa dal proxy same-origin /api/portals/[slug]/logo (nuovo route handler) → gateway.uploadPortalLogo (multipart con auth X-Tenant+cookie, no Content-Type forzato). Coerente con tutte le altre chiamate portali.
