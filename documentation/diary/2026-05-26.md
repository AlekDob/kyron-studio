---
type: diary
project: kyron-studio
date: 2026-05-26
---

- [23:55] (Alek) **WS-03 Phase 5b — Drawer responsive + agente piu' ambizioso**. (1) Bundle inline ridotto a top-3 + bottone "Vedi tutte (N)" che apre `AnnotationsDrawer` — fixed inset-0, su desktop slide da destra 480px, su mobile bottom sheet 88vh con corner radius top. Easing iOS `cubic-bezier(0.32, 0.72, 0, 1)` a 280ms, stagger fade-up 30ms sui rows, scale 0.995 active feedback. Esc chiude. (2) `AnnotationDetail` drawer annidato z-60 sopra il primo: header con Indietro/Elimina, dettaglio completo (tipo, pagina, selector, originale, proposto, immagine, nota, posizione, meta). Stessa animazione del parent. (3) Prompt agente esteso: spinto a proporre attivamente `replace-image`, `restructure` (layout 5 col, masonry, hero photo, riorganizzazione card), `add-section` (testimonial, gallery, stat, FAQ). Esempi concreti nel prompt per layout + immagine. Suggerimenti proattivi a 2-3 alternative quando l'utente e' vago. KEY INSIGHT: drawer fixed + responsive con stesso componente per mobile/desktop = una sola animazione da curare. Stagger su entry list + active scale + iOS curve trasformano una lista CRUD piatta in qualcosa che si sente "vivo". Le proposte strutturali sopravvivono al canale .md perche' la `proposal.note` accetta descrizione libera che lo sviluppatore puo' leggere e interpretare — non serve un DSL per il layout.
- [23:30] (Alek) **WS-03 Phase 5 — Annotazioni chat-integrated**. Eliminato il popover `ReviewComposer` quando l'overlay cms gira dentro l'iframe studio. (1) cms `ReviewOverlay`: handshake postMessage (`kyron-rev:hello` → `kyron-rev:ack` con origin whitelist `studio.kyronedu.it`/`localhost:3010`), embedded mode nasconde Bar+Composer e posta solo eventi `select`/`hover`/`clear` al parent con `target` + `rect` (BoundingClientRect). (2) studio `PreviewWorkspace`: listener postMessage con whitelist `staging.kyronedu.it`, state `pendingTarget`, due div assoluti sopra l'iframe per hover/selection outline (cross-origin clean: il cms manda solo coordinate, lo studio disegna fuori). (3) studio `PreviewChat`: chip `SelectionChip` sopra il composer mostra path + testo selezionato + X per deselezionare; `pendingTarget` passa in `context.pendingTarget` allo stream agente. (4) `ProposalCard` inline: quando l'agente chiama `propose_annotation`, il feed chat renderizza un bubble Originale/Proposto/Nota con bottoni Conferma/Modifica/Annulla — su Conferma il client `onAdd()` immediato (zero latenza) + manda user message sintetico "Confermo, aggiungi al bundle" all'agente per tenerlo allineato (pattern human-in-the-loop). (5) studio-server: prompt aggiornato per usare `ELEMENTO SELEZIONATO` come fonte canonica di page/selector/original.text. Output finale invariato: bundle .md → Resend → Alek. KEY INSIGHT: separare *commit lato client* da *sync agent state* via synthetic user message permette UI istantanea senza rompere la coerenza dell'agente — la proposta vive come tool-call bubble dentro il feed, conferma = side-effect client + segnale all'agente nel turno successivo.
- [22:30] (Alek) **Fix post-deploy WS-03**. Due gotcha catturati live: (1) Redirect loop `/login` ↔ `/login`: il root layout faceva auth gate su TUTTE le route, anche `/login`. Refactor con route group `(authed)/`: layout autenticato wrappa `/`, `/dati`, `/preview`, `/schools`, `/settings`; `/login` e `/api/*` restano fuori. (2) Submit del form login redirigeva a `0.0.0.0:3010` (ERR_SSL_PROTOCOL_ERROR): è la stessa gotcha cms feature 025 — dietro Coolify+Traefik `req.url` legge il `HOSTNAME` del container. Fix: helper `redirectBase(req)` che usa `NEXT_PUBLIC_SERVER_URL` (settato su Coolify), fallback a `new URL(req.url).origin`. Applicato in `api/login/{request,verify}/route.ts`. Aggiunta env `NEXT_PUBLIC_SERVER_URL=https://studio.kyronedu.it`. KEY INSIGHT: il pattern "root layout fa auth + DesktopShell" è popolare ma ROMPE le route pubbliche perché non c'è modo di skipparle da layout. Route group `(authed)/` è la soluzione canonica Next.js App Router.
- [22:00] (Alek) **Studio standalone — Workstream 03 Phase 1-4 done**. Quattro fasi in sequenza per rendere studio.kyronedu.it autonomo dal cms. (1) Port OTP login: `lib/otp.ts` + `app/api/login/{request,verify}/route.ts` + pagina `/login` 2-step (email → codice 6 cifre via Resend). Cookie `kyron-rev` su `.kyronedu.it` cross-subdomain, segreto HMAC condiviso col cms (`PAYLOAD_SECRET`) per transition compat. (2) Port review lib (`types`, `urn`, `exportMarkdown`) con nuovo `kind:"dom"` per cattura iframe (URL + selettore CSS); API `/api/review/send` usa `getCurrentUser()` + Resend con attachment .md. (3) Nuovo agente AI **Review Editor** (studio-server/features/review-editor) con tool `propose_annotation`/`add_annotation`/`request_send_bundle`. Frontend `/preview` con split-pane iframe sx (URL bar normalizzante) + chat dx; il client intercetta `add_annotation` e accumula bundle in React state, bottone "Invia via email" POST a `/api/review/send`. Nuovo modulo "Anteprima" in sidebar (Eye icon, viola). (4) Redirect 308 in `cms/middleware.ts` da `/studio/*` `/it/studio/*` `/en/studio/*` → `studio.kyronedu.it/<tail>`. KEY INSIGHT: l'agente Review Editor è **stateless**, lo stato delle annotazioni vive in React useState lato client — i tool ritornano payload formattati che il client interpreta. Limite MVP: cross-origin iframe → no selector overlay live (richiede injection cms-side con postMessage, roadmap successiva).
- [20:10] (Alek) **Search + paginazione lista record**. Aggiunta barra ricerca (form GET → `?q=`) e nav prev/next sulla pagina `/dati/[slug]`. Limit a 25/pagina. `buildHref` helper omette `page=1` per URL puliti. Stato disabled per prev/next quando fuori range. Reset link visibile solo con query attiva. Server-side render puro.
- [19:30] (Alek) **Modulo Dati + chat agente Editor Dati live**. Creata feature 003-dati-module che documenta l'intera Phase 2+3 del workstream 02 cross-progetto: 3 route (`/dati`, `/dati/[slug]`, `/dati/[slug]/[id]`), form generico con type detection esteso (string/text/number/date/boolean/localized IT-EN/relation chip readonly/relations multi-chip/json fallback), split-pane `DataWorkspace` 1fr/420px lg+, `DataChat.tsx` con auto-navigation su `get_record`/`update_record` (router.push + skeleton) e auto-refresh immediato al tool-result mutating, skeleton loading.tsx su lista e detail, dev-cookie auto-signing in `lib/gateway.ts` + proxy `/api/agent/data-editor`. Aggiornato CLAUDE.md: sidebar a 7 moduli, link a feature 003. KEY INSIGHT: il loop "AI as collaborator" si chiude quando il tool-result mutating triggera router.refresh() — l'utente vede la modifica nella form senza azione manuale. Senza questo, la chat e la form vivono in mondi separati.
- [15:10] (Alek) Studio settings: layout tabs Virgilio-style (feature 002).
- [14:30] (Alek) Studio shell port da Virgilio Phase 1 (feature 001).
