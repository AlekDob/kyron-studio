---
type: diary
project: kyron-studio
date: 2026-08-19
---
- [15:40] (Alek) Redesign completo dello Studio sul design di global-games: scrivania grigia con due sfere indaco, sidebar trasparente, contenuto su lastra di vetro, card bianche piatte, accento indaco `#5b67f0`, dark mode rimosso. KEY INSIGHT: i due progetti sono gemelli tecnici (Next 16, Tailwind v4 CSS-first, kit UI a mano) e lo Studio aveva 1.364 `className` che leggevano gia' i token per nome — cambiando solo i **valori** in `globals.css` si sono ridisegnati ~100 componenti senza toccarli. File modificati a mano: 15. Vedi feature 014.
- [15:40] (Alek) Trovato un bug latente pre-esistente: `--radius-control` era referenziato in 34 punti ma **mai definito** — quei bordi venivano quadrati. Definito a `0.625rem`.
- [15:40] (Alek) Font: `Geist`, `Instrument Serif`, `JetBrains Mono` erano solo stringhe in `globals.css`, senza `next/font`. L'app ha sempre girato col font di sistema. Ora `layout.tsx` carica davvero Plus Jakarta Sans + Doto, variabili su `<html>` (non su `<body>`, altrimenti `--font-dots` non risolve da `:root`).
- [15:40] (Alek) GOTCHA Lightning CSS: scrivendo a mano sia `backdrop-filter` che `-webkit-backdrop-filter`, Lightning (dentro Tailwind v4) scarta la standard e tiene solo la prefissata — che Chromium non applica: lastra senza blur. Scritta solo la forma standard, Lightning aggiunge il prefisso da solo. **Lo stesso bug e' in `global-games/studio`**, ha il CSS identico.
- [15:40] (Alek) GOTCHA containing block: `backdrop-filter` sulla lastra la rendeva containing block per i figli `position: fixed` — drawer ordini, modali e drawer annotazioni si ancoravano alla lastra (248,16 1184x868) invece che alla finestra. Spostato il blur su `.studio-content-inset::before` con `z-index:-1`: verificato che un `fixed` figlio torna a 0,0 1440x900.
- [15:40] (Alek) La lastra e' alta `100dvh - 32px`: le 7 pagine con `min-h-screen` sforavano di 32px creando uno scroll fantasma. Passate a `min-h-full` (`/login` resta `min-h-screen`, sta fuori dalla shell).
- [15:40] (Alek) Contrasto: `--studio-muted-label` alzato da 48% a 58% di nero (global-games usa 48%) — sul grigio della lastra il 48% sta a 3.3:1 e le `.eyebrow` sono testo, non decorazione. Ora 4.5:1. Anche `--color-ink-muted` alzato a `#5f636e` (5.0:1). Barre degli skeleton da `--color-line` a `--color-line-strong`: su lastra grigia erano invisibili.
- [15:40] (Alek) Verificato: `tsc --noEmit` pulito, nessun residuo di `data-theme`/`useTheme`, scroll ok su /analytics /orders /preview a 1440 e 390, nessun overflow orizzontale, blur attivo solo da 1024px (mobile opaco, i due gotcha iOS restano validi).
- [16:55] (Alek) `/portals` andava in 500 in locale: la pagina chiamava `listPortals()` senza try/catch, quindi un errore del BFF buttava giu' tutta la route. Ora cade su lista vuota come fa `/orders`. La causa a monte non era il codice: sulla porta 8790 girava il **studio-server di global-games** (stessa porta) e su 3000 un altro progetto, non il CMS Kyron.
- [18:20] (Alek) Chat agenti ridisegnate sullo stile Virgilio + avatar blobatar come in `studio-futuro` (feature 015). Le risposte dell'agente erano testo nudo: ora hanno avatar 28px e nome. La bolla resta solo all'utente.
- [18:20] (Alek) Installato `blobatar` (v2, zero dipendenze). Hue bloccata a 268 (indaco Studio) e tone libero: cosi' ogni agente ha una sua sfumatura dentro la palette. Bloccare anche il tone li rende tutti identici di colore (`#c2d4ff`), verificato. Avatar statici: `animate` obbligherebbe a SVG inline, ~12 nodi DOM per messaggio.
- [18:20] (Alek) Estratto `ChatComposer` unico: le 6 chat si copiavano lo stesso composer con differenze casuali (Portali aveva la freccia in SVG a mano, Onboarding l'unico `focus-within`). Ora e' una card `rounded-[20px]` con ombra e freccia lucide, come Virgilio. Cambiare stile alla chat non e' piu' sei diff identici.
- [18:20] (Alek) L'indicatore "sta pensando" da rotella che gira a tre puntini sfalsati (studio-futuro). Il nome dell'agente e' anche il seed dell'avatar: rinominarlo cambia la faccia.
- [18:45] (Alek) Avatar chat da 28 a 36px e un colore diverso per agente: tolto il lock sulla hue (era l'indaco per tutti) e bloccato il `tone` a 0.45. Il tone libero manda alcuni nomi quasi al nero (`Portali` dava `#213d41`), a 0.45 escono sei pastelli pieni con la stessa luminosita'.
- [19:05] (Alek) Logo sidebar: via il quadratino "K", dentro `StudioMark` portato da global-games — sfera indaco con cinque dischi che cadono e si dissolvono in loop. Keyframes `studio-mark-disc` aggiunti a `globals.css`, fermi con `prefers-reduced-motion`.
