---
type: diary
project: studio
date: 2026-08-27
---

- [10:15] (Alek) Browser al 100% di CPU sulla dashboard, anche a schermo fermo: la causa erano gli avatar degli agenti. Con `gaze` la faccia diventa SVG inline e `blobatar/motion.css` le attacca 5 animazioni `infinite` che girano sempre — `mo-shake` ogni 112ms, `mo-rock` che anima una custom property ereditata (invalida lo stile di tutto il sottoalbero a ogni frame), `mo-wrap`, `mo-breathe`, `mo-bob` — invisibili ma pagate. La dashboard le moltiplicava: `AgentsMarquee` stampa gli agenti DUE volte (11 -> 22 facce, ~130 animazioni) e scrive `scrollLeft` 60 volte al secondo per sempre, e ogni repaint sotto `.studio-content-inset::before` costringe a ricalcolare il `backdrop-filter: blur(24px)` di tutta la lastra. Quattro fix: `gaze` solo su /agenti (dashboard = `<img>` statici, verificato: 0 `mo-root` nell'HTML), animazioni blobatar spente in `globals.css` tenendo solo il blink, `aim()` legge tutti i `getBoundingClientRect` prima di scrivere (prima alternava read/write = un layout forzato per faccia a ogni `pointermove`), rAF del marquee in pausa via IntersectionObserver quando la riga esce dal viewport. Verifica rapida in console sulla dashboard: `document.getAnimations().length` (prima 100-150).

- [11:05] (Alek) FUT-81 — Ada disegna grafici veri, e non solo lei. La card grafico e' finita in `@studiofuturo/studio-core` (`ChartCard`, v0.3.0) con cinque tipi: `timeline` (area), `columns` (istogramma verticale), `bars` (barre orizzontali per etichette lunghe), `pie` (ciambella + percentuali), `table`. Sotto il grafico resta sempre la tabella. La scelta di progetto che ha risparmiato piu' codice: **niente nuovo formato dati** — la card legge la stessa tabella `columns` + `rows` che torna da `runHogqlWithColumns`, prima colonna = etichetta, ogni colonna numerica successiva = una serie. Cosi' il multi-serie (`giorno | luglio | giugno`) esce gratis, senza un parametro in piu'. `StatsResult.tsx` cancellato: la card fa tutto quello che faceva. Lato agente `run_hogql` ora emette `_ui.component = "Chart"` e c'e' un tool nuovo `render_chart` per i dati che Ada ha gia' in mano (overview, campagne Meta) senza rifare una query. `chartTool` esiste anche nel core per gli altri Studio, ma studio-server lo ridichiara in locale: il descriptor e' 4 righe, mentre la dipendenza dal core gli costerebbe l'auth GitHub Packages nella build Docker (il suo Dockerfile non ha `ARG NPM_TOKEN`, quello di `studio` si'). Coro (`wacebot`) resta col suo `render_plot` su Observable Plot: runtime diverso (`pi` agent, agenti come righe DB, tool JSON Schema globali) e i grafici li pubblica come blocco messaggio, non come tool result — "Ada dentro Coro" e' un ticket a parte, il seam e' `AgentRuntime`/`ConnectorTool` in `packages/adapter-kit`.


- [11:19] (Alek) FUT-82 — pannello Catalogo rifatto sul modello di global-games. Le tre cose che lo rendevano illeggibile erano dati, non stile: lo slug del canale al posto del nome della scuola, "Pubblicato su" come riga di slug separati da virgola, e il magazzino come unico numero in lista (informazione che non serve a chi guarda il catalogo). Ora: anteprima thumbnail Saleor per riga, ricerca fuzzy (riuso `lib/fuzzy.ts`, gia' scritto per il breakdown Analytics: "mcbk air" trova MacBook Air), ordine "pubblicati prima, piu' venduti sopra", skeleton al posto del vuoto, stagger `.studio-row-in` sulle prime 8 righe, pannello su lastra di vetro. Il magazzino scende nel drawer e in lista al suo posto ci sono le **vendite**: nuovo `commesso/sales.ts` che pagina gli ordini Saleor con una query leggera (solo `status`, `userEmail`, `channel.slug`, `lines{productSku quantity}` — la ORDERS_QUERY del modulo Ordini porta indirizzi, metadata e transazioni che qui non servono), aggrega per SKU con totale + per canale, esclude annullati e le email di test (stessa env del report giornaliero), cache in memoria 15 minuti. I nomi dei portali arrivano da Payload (`listPortals().nome`, lo stesso join del modulo Ordini) col nome canale Saleor come fallback per il main shop e i pilot senza doc Payload. Un solo endpoint nuovo, `GET /api/v1/products/insights`, che torna nomi + vendite: il pannello fa una fetch di contorno e se cade resta in piedi con gli slug e le vendite a zero. Nel drawer "Pubblicato su" e' diventata una lista ordinata per vendite con nome scuola, prezzo su quel portale ("da X" quando le varianti hanno prezzi diversi) e pezzi venduti, con la sua ricerca fuzzy da 6 portali in su.
- [12:05] (Alek) FUT-82 secondo giro — Nico non elenca piu' i prodotti in chat: regola ferrea nel prompt + `experimental_toToolResultContent` su `list_products`/`get_product` (al client il result intero per le thumbnail, al modello una copia senza `imageUrl`/`description`/`id`). Insight: le "immagini enormi" non erano un problema di CSS ma di contesto — dando gli URL al modello, lui li incolla. Rete di sicurezza `.chat-md img` a 140px per tutte le chat. Nel pannello il "da X €" ora apre un popover con prezzo per variante (`VariantPricesPopover`, div assoluto: il `[popover]` nativo avrebbe richiesto il fix del preflight Tailwind).
- [12:40] (Alek) Drawer prodotto ripulito: sezioni separate da una linea (`divide-y`) e rinominate in Informazioni / Portali (N) / Varianti (N), stesso stile `Section` del drawer ordini. Le varianti non sono piu' un blocco verticale a testa (12 tagli = tabella infinita): una riga di pallini colorati in flex-wrap, il dettaglio (colore, codice, venduti, magazzino) nel popover. Nuovo `ui/Popover.tsx` riusabile: contenuto in portal su `document.body` con `position: fixed` calcolata dal rect del trigger, si flippa sopra se sotto non c'e' spazio, chiude su click fuori/Escape/scroll. Insight: dentro il drawer un popover `absolute` viene tagliato dal contenitore scrollabile, e un `fixed` non salva perche' l'aside ha un `transform` (che diventa containing block) — il portal e' l'unico modo per averlo sempre visibile. Ricerca portali resa piu' evidente (input piu' grande, bordo forte, sfondo pieno).
- [12:58] (Alek) Popover prezzi per variante: etichetta = capacita', non colore. Mostrava "Blu 509, Rosa 509, Argento 509, Blu 639..." perche' su Saleor il prezzo dipende dal taglio e i quattro colori dello stesso taglio costano uguale. Ora una riga per taglio (128GB 509, 256GB 639, 512GB 889), duplicati uniti in una Map.
- [13:10] (Alek) Rimossa la sezione Varianti a pallini colorati dal drawer: con 4 colori x 3 tagli erano 12 pallini con i colori ripetuti tre volte, non dicevano nulla. Il dato utile (prezzo per taglio) sta gia' nel popover dei portali. `ui/Popover.tsx` resta, lo usa il popover prezzi.
- [14:05] (Alek) Drawer portale nuovo, con la UI del catalogo, e i due moduli finalmente collegati. Dal drawer prodotto il nome del portale e' cliccabile e apre il portale **sopra** (`PortalDrawer`): il primo drawer del repo che usa `Drawer` di `@studiofuturo/studio-core` invece di riscrivere il boilerplate — fa `createPortal` su body con z-80, mentre i tre drawer scritti a mano (catalogo, ordini, anteprima) sono `fixed z-50` inline nell'albero e condividono un `<style>` con selettore globale, quindi due istanze si pesterebbero. Il bottone nome e' **fratello** del popover prezzi, non un wrapper della riga: il popover e' un bottone e annidarli sarebbe HTML invalido. Nuovo `PortalCatalogSections`, un solo componente per i due host (pannello portali editabile, drawer catalogo read-only): i chip con lo slug nudo sono diventati righe con thumbnail, nome vero, prezzo su QUEL canale e pezzi venduti; sconti e tagli pubblicati non sono piu' due sezioni a parte ma badge di riga. Zero endpoint nuovi: prezzo per canale e vendite arrivano da `/api/products` + `/api/products/insights` via `use-catalog-index` (una fetch per sessione, Promise in cache a livello di modulo). Il prezzo per canale lo ricava da `portalRows(product, {}, sales).find(r => r.slug === channel)`, che restituisce anche il flag "da X" e le vendite in un colpo: nessun helper nuovo. Il pannello portali ha perso le card con bordo (ora `divide-y` + `Section` di `drawer-primitives`, come il drawer prodotto) e 250 righe: `CatalogEditor` e gli helper kit sono usciti da `PortalDetail.tsx` (840 -> 589 righe, sotto la soglia). Non mostrato di proposito: il **voucher reale** dei kit, che sta su Saleor e lo legge solo Price Guard — servirebbe un endpoint nuovo (`fetchProduct` + `readVoucherDiscount` esistono gia', si compongono se serve).
- [14:52] (Alek) Dashboard sforava in larghezza da mobile: la cella del grid con la striscia agenti (marquee, contenuto `w-max` ~2300px) faceva da larghezza minima alla colonna, quindi TUTTE le tile diventavano larghe 2200px. Fix = `min-w-0` sulla cella. Misurato a 402px: colonna 2320px prima, 354px dopo, zero elementi fuori viewport. Insight: non si vedeva da desktop perche' li' la colonna e' 1/12 e il vincolo non morde.
- [15:14] (Alek) Tile Fatturato con periodo scegliibile: Sempre (di base), 7 giorni, 3 giorni, Oggi, salvato in localStorage. I 4 totali li calcola il server da UNA sola lettura ordini (`ordersAll`, storico intero) e li passa gia' pronti, quindi lo switch e' istantaneo e senza rete; ordini-30gg e grafico si ricavano filtrando la stessa lista. Insight: la somma per periodo replica esattamente il reduce del gateway, cosi' i numeri non divergono dalla lista ordini. Segnato il debito: il gateway pagina a 100 ordini, prima o poi serve un endpoint di aggregati.
- [15:26] (Alek) Scoperto verificando: in locale la dashboard NON si idrata quando studio-server non raggiunge Saleor/Payload — i Suspense restano nel fallback e i bottoni non rispondono. Si riproduce identico su `main` con lo stash applicato, quindi e' l'ambiente, non il codice. Per provare interazioni sul cruscotto serve un gateway con le fonti vive.
- [16:20] (Alek) Portale: Prodotti e Kit ora in un unico elenco con la stessa grafica di riga. Nuovo `KitThumbnail` = miniatura del collage storefront (`BundleHeroCollage`: hero grande + accessori a destra), `PortalKitRow` porta tutto l'editing del vecchio `BundleCard` (nome, prezzo finale, componenti, elimina) dentro una riga: i componenti si aprono SOTTO la riga, non in popover, perche' `ui/Popover` si chiude allo scroll e la lista prodotti scrolla. `InlineText`/`InlinePrice` estratti in `inline-fields.tsx` (servivano a due file). `PortalDetail.tsx` da 617 a 211 righe.
- [drawer ordini] (Alek) Il drawer dettaglio ordine si apriva tagliato e scorreva con la lista: non era il drawer ma `.page-enter` (studio-core, `fill: both`) che a animazione finita lascia il transform come MATRICE identita' invece di `none` — e una matrice identita' rende il wrapper containing block per tutti i figli `position: fixed`. Fix: override `animation-fill-mode: backwards` in globals.css. Vale per ogni drawer/modale sotto (authed).
- [17:05] (Alek) Fix miniature kit vuote: in un box 36px un `<img max-h-full max-w-full>` dentro un flex collassa a 0x0 (verificato in browser: 0x0 vs 21x36 con absolute). `KitThumbnail` riscritto con posizionamento absolute + fallback dashed come `ProductThumbnail`. Hero del collage = il componente multi-taglio (selection by-attribute = iPad/Mac), altrimenti in cover finiva un alimentatore.
- [dashboard] (Alek) Tile Fatturato alta come le altre tre: la fila di pastiglie periodo andava a capo e alzava la tile. Ora il periodo si scegle da un popover (riuso `ui/Popover`, che ora accetta anche `children` come funzione `(close) => ...` per chiudersi alla scelta) e c'e' anche "30 giorni" (`RANGE_DAYS["30d"]`). Verificato in browser: le quattro tile misurano 167px.
- [17:25] (Alek) Miniature kit e prodotto ora identiche: stesso box 36px, stesso ring, stessa aria interna (p-1). Verificato in browser: box 36/36, immagini inset.
- [18:10] (Alek) Studio su iPhone: shell aperta sotto e tutti i drawer dal basso. Tre cose. (1) La lastra `main.studio-content-inset` era una card chiusa su 4 lati: il radius in basso tagliava il contenuto e faceva sembrare la pagina finita. Ora sotto i 1024px ha radius solo in alto e scende fino al bordo schermo (via il `padding-bottom` del wrapper con `div:has(> main.studio-content-inset)` — il pacchetto non espone una prop) e la cornice, che era un `box-shadow` ring e quindi non si taglia per lato, e' rifatta `inset` su top/left/right. Gli 8px laterali restano. Scoped a `main` di proposito: `/login` usa la stessa classe su un `div` e deve restare card. (2) Il bug vero: nel pannello di Nico toccare un prodotto non faceva niente. Non era l'evento — `MobileChatOverlay` era un portal su body a `zIndex 60`, `ProductDrawer` un `z-50` renderizzato in place, quindi il dettaglio si apriva DIETRO il pannello (chiudendo con la X ricompariva: era la' da subito). Fix = tutti i drawer sul `Drawer` di studio-core, che fa portal su body con z 70/80 uguali per tutti, cosi' e' l'ordine di apertura a decidere chi sta sopra. (3) Tutti i drawer sono bottom sheet su mobile e da destra su desktop, con nuovo `lib/use-is-mobile.ts` perche' `side` e' una prop, non una classe. Bilancio: 962 -> 647 righe sui 5 componenti toccati, spariti 3 `<style>` con l'hack del translate desktop e tutta la macchina di animazione locale (il `Drawer` cachea i figli durante l'uscita, quindi via `render`/`show`/`current` e i loro effect). Riusato `studio-row-in` invece di riscrivere un keyframe. Un `!important` in `globals.css` porta l'altezza sheet a `dvh`: studio-core 0.3.0 la scrive inline in `vh` e su iOS finirebbe sotto la notch — il fix vero e' nel pacchetto. Effetto collaterale accettato: con due sheet impilate `Esc` chiude entrambe. Verificato in browser a 390x844 (radius `20px 20px 0 0`, fondo a 844, sheet da 124px al bordo): le animazioni non si vedono in WebView di background perche' con `document.hidden` il doppio rAF non gira.
- [18:35] (Alek) Sidebar mobile allineata al taglio della shell: era `fixed inset-y-0 left-0 w-[280px]` con gli spigoli vivi attaccati ai bordi, e accanto alla lastra arrotondata stonava. Ora prende gli stessi stacchi (8px a sinistra, 16px in alto), radius solo in alto e fondo aperto. Override in `globals.css` sui selettori Tailwind del pacchetto: `div.fixed.inset-y-0.left-0.w-\[280px\]`. Il dettaglio non ovvio e' la chiusura: con lo stacco di 8px il `-translate-x-full` del pacchetto lascerebbe fuori una fetta di 8px, quindi da chiusa la traslazione diventa `calc(-100% - 0.5rem)`. Verificato a 390x844: top 16, left 8, radius `20px 20px 0 0`, altezza 828. Nota di processo: la regola non arrivava al browser (chunk CSS con lo stesso hash) — e' il gotcha Turbopack, serviva `rm -rf .next` + restart del dev server.
- [18:50] (Alek) Trovato perche' il popover del periodo non si apriva da desktop e da mobile si': la tile ha `whileHover` (y/scale) + tilt 3D, quindi tra mousedown e mouseup la pastiglia si sposta e il browser manda il click all'antenato comune, mai al bottone. `Popover` ora apre su `pointerdown` (overlay che chiude su pointerdown, `onClick` solo per la tastiera con `detail === 0`). Vedi gotcha-click-perso-su-card-con-tilt.
- [18:55] (Alek) Selettore di periodo su tutte le tile del cruscotto tranne Portali attivi (che e' un conteggio di adesso, non ha periodo). Estratto `RangePicker` + `useStoredRange`; `RevenueTile` diventa `BucketTile` e serve sia Ordini (conteggio) sia Fatturato (lordo) dalla stessa lettura ordini. Visite ha periodi PostHog (Oggi/7/30/90) via server action `visitsTotalsAction` con cache client per periodo: la Query API sta a ~120 query/ora condivise con /analytics, quindi il periodo delle visite non si ricorda tra un caricamento e l'altro.
- [Nico file Danea] (Alek) Fix del falso "Import scaduto o non trovato" dopo una domanda intermedia. `toApiContent` manda al primo turno il JSON della card con `importId`, ma `useAgentStream` conserva nella cronologia solo la frase visibile (nome file e conteggi): Nico mantiene il contesto umano, pero' al turno successivo perde l'id casuale dello store in memoria. Fix locale in `CatalogoWorkspace`: l'ultima submission valida di `DaneaUploader` resta in un ref e `selectionContext` aggiunge id, tipo e filename al solo contenuto API di ogni turno seguente; un nuovo upload sostituisce il precedente, refresh e TTL restano invariati. Non pubblicare per ora un fix in `@studiofuturo/studio-core`: il repository `main` dichiara ancora 0.2.4 e non contiene parti gia' distribuite nel pacchetto 0.3.0 (grafici generativi e chart tool). Prima di una nuova release va riallineato il sorgente del core al pacchetto pubblicato.
- [Nico anteprima DDT] (Alek) La card del piano comunicazione distingue ora il destinatario della **prova** dai clienti usati per l'anteprima. Header stile Mail con `A:` editabile, `Oggetto:` e rassicurazione esplicita: parte una sola mail e solo a quell'indirizzo. Le tre email sopra l'iframe sono etichettate come dati cliente dell'anteprima e non come destinatari. Il logo rotto era solo nell'iframe: l'HTML usa `cid:kyron-logo`, valido nella mail Resend con allegato inline ma irrisolvibile in `srcDoc`; per la sola preview viene sostituito con `https://kyronedu.it/kyron-logo.png` (200 verificato), mentre l'invio vero conserva il CID.
- [17:10] (Alek) Un solo selettore periodo sulla dashboard, in alto a destra, default "Da sempre". Le tre pastiglie sulle tile (Ordini / Fatturato / Visite) spariscono: stesso `RangePicker`, stato unico in `studio.dashboard.range`. PostHog non ha lo storico infinito, quindi "Da sempre" sulle visite e' 90 giorni. Il grafico resta a 30 giorni. Verificato in browser: un bottone solo, label "Da sempre"; i numeri delle tile restano "—" (gateway locale senza Saleor/PostHog, stesso limite di prima).
- [17:17] (Alek) Dashboard periodo globale in prod: commit `f6d13ad` pushato su `main`, Coolify studio-staging `finished` (commit allineato). Live su studio.kyronedu.it.
- [17:35] (Alek) Popover periodo: da Chrome desktop flickera (apre e chiude nello stesso click), da Safari mobile no. L'overlay a schermo intero si monta sopra il trigger durante il `pointerdown`; Chrome ritargetta il pointer li' e lo stesso gesto chiude. Fix: `hold` fino a `pointerup`, l'overlay ignora quel gesto.
