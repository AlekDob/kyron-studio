---
type: diary
project: kyron-studio
date: 2026-08-25
---
- [14:20] (Alek) Home `/` da launcher a **cruscotto**: 4 tile (Ordini 30gg, Fatturato 30gg, Portali attivi, Visite 30gg) + grafico traffico + striscia agenti. Insight: gli skeleton non servono stato client — async server component dentro `<Suspense>` e Next streamma le tile una alla volta. PostHog chiamato una volta per due componenti con `cache()` di React. Cancellato `src/components/Dashboard.tsx` (ricerca duplicata di Cmd+K).
- [14:20] (Alek) I 5 agenti hanno un nome proprio: **Livia** (Portali), **Nico** (Dati), **Bruno** (Controlli), **Elsa** (Agevolazioni), **Vera** (Anteprima). Fonte unica `modules.ts` con label doppio `Livia · Portali` + `agentNameOf(id)`: prima il nome era una stringa copiata a mano in 9 file. Effetto gratis: blobatar e' seedato sul nome, ogni agente ha ora la sua faccia stabile. Rinominata anche l'auto-presentazione nei 5 prompt di studio-server — **serve redeploy** per vederlo in prod.
- [14:20] (Alek) Nuova pagina `/agenti` (directory, fuori da `MODULES` per non doppiare le righe di sidebar) con la `AgentCard` di studio-core, mai usata prima in Kyron. Vedi `documentation/features/016-studio-dashboard-cruscotto.md`.
- [15:05] (Alek) Aggiunto il grafico **Ordini e fatturato per giorno** accanto alle visite (mezza riga ciascuno). Non l'ho messo nella stessa area delle visite: 2 ordini e 3.000 euro sull'asse dei visitatori (centinaia) schiacciano la linea ordini a zero — per lo stesso motivo il chart ordini ha due assi Y separati. Nessun endpoint nuovo: `listOrders` restituisce gia' le righe, il raggruppamento per giorno lo fa `OrdersSection` e la chiamata e' condivisa con le tile via `cache()`. Gotcha: in un browser headless/in background le tile e le aree sembrano vuote perche' con `document.hidden` l'animazione di mount non parte — la geometria nel DOM c'e'.
- [15:40] (Alek) Corretto il giro precedente: **un grafico solo**, non due affiancati. Il confronto che serve e' visite contro ordini, quindi le due serie stanno nello stesso chart con gli ordini su un asse Y a destra (tratteggio ambra). Il fatturato non e' una terza linea — gli euro non condividono scala — sta nel tooltip. `TrafficChart` prende una prop `orders` opzionale: su `/analytics` resta undefined e il grafico e' quello di prima. Cancellati `OrdersChart.tsx` e `OrdersSection.tsx`.
- [16:10] (Alek) Font cambiati: Plus Jakarta Sans -> Geist (testo), Georgia di sistema -> Instrument Serif (corsivi dei titoli). Il vecchio abbinamento sembrava quello di Claude. `--font-serif` va ridefinito in globals.css: theme.css non puo' conoscere la variabile che next/font genera nell'app.
- [16:35] (Alek) Corretto: il "sapore Claude" era il corsivo serif nei titoli, non il sans. Instrument Serif somigliava a Copernicus piu' del Georgia di prima. Ora font unico (Geist): la seconda parola del titolo resta normale e grigia invece di corsiva serif. `--font-serif` non serve piu', tolto anche l'import del font.
- [17:05] (Alek) Sidebar sfoltita: via Inbox, Brain e Log (erano placeholder "presto", il lavoro vero sara' un prodotto a parte). E Nico torna in Strumenti come "Dati": non e' un agente autonomo, e' la chat dentro lo strumento. Tiene `agentName` per la chat, quindi `AGENTS` ora filtra anche su `kind === "agent"` o ricomparirebbe nella griglia.
- [18:30] (Alek) Nuovo agente **Ada · Statistiche** (`/stats`): interroga PostHog scrivendo HogQL da sola e risponde con testo + tabella + grafico. Il vincolo che ha guidato tutto e' il rate limit della Query API (~120 query/ora per key, condivisa con `/analytics` e il report delle 09:00): le domande standard passano dal tool `overview` che legge la cache esistente, e Ada ha un budget suo di 40 query/ora. Guard `assertReadOnly` (solo SELECT/WITH, una istruzione, lista nera DDL/DML, no `url()/file()`, `LIMIT 200` forzato) con 13 test verdi. Aggiunto `runHogqlWithColumns` accanto a `runHogql` senza toccarlo: senza i nomi delle colonne tabella e grafico non hanno etichette. Vedi `documentation/features/017-stats-agent.md`. Nota: la chat non l'ho potuta provare in locale, la key OpenAI di dev e' senza credito — il giro proxy → studio-server → agente arriva fino al modello. Serve un redeploy di studio-server per averla in prod.
- [19:15] (Alek) Sidebar e chat agenti allineate allo Studio GGS. In sidebar gli agenti non sono piu' cinque voci di primo livello: c'e' una voce **Agenti** e sotto i canali `#` con nome + ruolo (`SubChannels` del core, prop `subChannels` di `DesktopShell`). Quindi `AGENTS` esce da `MODULES` — effetto collaterale accettato: i 5 agenti non compaiono piu' nella palette ⌘K, perche' `DesktopShell` usa la stessa lista per sidebar e palette (identico a GGS). Chat nuova a canale: `AgentChannel` (header `#nome` + "agent", stato vuoto con avatar grande + intro + suggerimenti, composer "Messaggio #nome") con `ChannelMessage` per la riga piatta stile Slack — avatar e nome solo sulla prima riga di un blocco, ora in hover sulle successive. Riusa `useAgentStream` del core, il `ChatComposer` di Kyron e il `GenerativeRenderer` locale, quindi `StatsChat.tsx` e `ChecksChat.tsx` sono cancellati (con loro `streamStats`/`streamPriceGuard` in `chat-runtime.ts`). Collegata per ora solo a `/stats` e `/checks`: il pannello destro dei Controlli continua a riempirsi da `onEvent` sui tool-result. Agevolazioni, Portali, Dati e Anteprima restano sulle bolle vecchie — la chat di Elsa manda un JSON al server mostrando una frase all'utente e `useAgentStream` non ha un gancio per quella divergenza.
- [20:40] (Alek) Occhietti che seguono il mouse. `AgentAvatar` del core rende un `<img>` con SVG in data-URI: dentro un'immagine il CSS della pagina non entra, quindi gli occhi non si possono muovere. Nuovo `AgentFace` che usa blobatar in modo animato (`animate="always"` = SVG inline, con `<g class="mo-eyes">` raggiungibile) e sovrascrive il saccade casuale di `motion.css` con due variabili CSS (`--gaze-x/y`, max 2.4 unita' di viewBox o le pupille escono dalla faccia). Un solo listener `pointermove` per tutta la pagina + un rAF di coalescing e scrittura diretta su `style`: in chat le facce sono decine, un re-render React per movimento del mouse no. Il `Pose` di blobatar non serviva: non ha un canale orizzontale. Rispetta `prefers-reduced-motion`. Nota: in una tab in background `requestAnimationFrame` non gira, quindi lo sguardo resta fermo — non e' un bug.
- [20:40] (Alek) Chat a canale ora su **tutti** gli agenti, non solo Ada e Bruno. `AgentChannel` generalizzato con quattro prop nuove: `interactive` (le card generative accettano input e rimandano i dati), `extraBody` (contesto di pagina per turno), `initialPrompt` (deep link da un altro modulo) e `hideCards` (le card che vivono nel pannello destro non si ripetono in chat). La divergenza "JSON al server, frase all'utente" che bloccava Elsa si fa con `toApiContent` del core + un ref che tiene il payload fino al send: nessun fork del hook. Un solo `describeSubmission` sostituisce i due formattatori copiati in PortalsChat e VatReliefChat. Cancellati `VatReliefChat.tsx`, `PortalsChat.tsx` e `streamVatRelief`; la logica che riempiva il pannello Portali (draft da args dei tool, risultati, submission e regex sul testo dell'agente) e' in `use-portal-draft-sync.ts`. Anteprima tiene il suo loop (proposte + chip di selezione + form manuale) ma prende la faccia GGS riusando `ChannelHeader` e `ChannelMessage`.
- [21:10] (Alek) Fix bug UI su `/preview` (Vera). Non erano tanti bug: era uno. La rail compatta, quando si espande in hover, e' un **overlay** sopra il contenuto (`absolute inset-y-0` dentro un box da 56px, cosi' non fa reflow) e la `Sidebar` del core e' `bg-transparent` per scelta — si appoggia alla scrivania grigia. Su `/preview` sotto c'e' un iframe con kyronedu.it, quindi il sito si leggeva attraverso le voci di menu: etichette illeggibili, l'header "Anteprima + url" che sembrava sovrapposto e un pallino rosso che non e' un badge ma un pezzo del sito sotto. Sistemato in **studio-core 0.2.4** (`bg-[var(--color-paper)]` + `shadow-[var(--shadow-modal)]` solo nello stato espanso, trasparente da chiusa) e non con un override CSS in Kyron: il difetto e' della shell e colpisce qualunque app con un iframe sotto. Tailwind v4 genera comunque le due utility perche' `globals.css` ha gia' `@source` sul dist del core (non scansiona node_modules da solo). Nota: il cerchio sfumato sotto il logo non e' un bug, e' la scia animata di `StudioMark`.
