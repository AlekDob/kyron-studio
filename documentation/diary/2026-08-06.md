---
type: diary
project: kyron-studio
date: 2026-08-06
---

- [13:36] (Alek) Modulo Risorse dentro Dati (`/dati/risorse`): lista con stato bozza/pubblicata, crea, modifica, elimina con conferma, upload PDF + copertina dal browser. Andrea non deve piu' entrare in `/admin` Payload. INSIGHT: il segmento statico `dati/risorse` batte il dinamico `dati/[slug]` in App Router — schermata dedicata senza toccare il renderer generico, e la card in `/dati` ci atterra da sola. Il generico non poteva bastare: `data-fields.ts` deduce il tipo dal valore, quindi select→testo libero, array→JSON, upload→sola lettura, e non esisteva nessuna pagina "crea record". Vedi feature 013.
- [13:36] (Alek) studio-server: `uploadMedia()` estratta da `portals/logo.ts` in `core/payload/media.ts` e nuova route `POST /api/v1/media` (whitelist mime, 25 MB). Trovato e corretto un bug latente: `gateway.create()` serializzava i campi localizzati `{it,en}` dentro la locale di default — stesso bug gia' noto su `update()`. Ora crea con la sola `it` e completa con `update`. Vale anche per il tool `create_record` dell'agente chat.
