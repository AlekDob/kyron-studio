---
type: diary
project: studio
date: 2026-06-08
---
- [Studio] (Alek) Aggiunto `e.silvestri@kyronedu.it` all'allowlist OTP `KYRON_REVIEW_EMAILS` di studio (studio.kyronedu.it) via Coolify API (env runtime production `v7uzacbm7rfbcamg8nhmsigi`, non la preview `toueirx...`). Lista ora: r.russo, a.maricelli, alekdobrohotov, a.ravelli, e.silvestri. Redeploy triggerato (`xtqlqmxhqra074lgtsjdvpg2`). Procedura: Coolify API via SSH (`kyron-prod`, localhost:8000) perché l'API non è raggiungibile dalla mia rete.
- [Studio+server+cms] (Alek) **Feature 008 — Utenti & ruoli da UI** (admin/editor). Allowlist migrata da env `KYRON_REVIEW_EMAILS` → collection Payload `studio-users` (email/role/isActive/invitedBy). Login: `resolveStudioAccess` → studio-server `GET /auth/resolve` (pre-login, no cookie) con fallback bootstrap `KYRON_ADMIN_EMAILS` (solo admin, anti-lockout se DB down). studio-server: `requireAdmin` (lookup ruolo fresco da DB) protegge `/api/v1/studio-users` (CRUD + guard "almeno un admin attivo") e ora anche `/settings/*`. Frontend: tab Impostazioni→**Organizzazione** (invita/ruolo/attiva/rimuovi); Connessioni/Modelli/MCP/Organizzazione admin-only (editor vedono solo Tema); `StudioUser.role` nel cookie per gating UI. Fix collaterale: proxy `/api/settings` e `/api/org` ora inoltrano il cookie kyron-rev (prima settings non lo faceva → con l'auth sarebbe andato in 401). Typecheck verde su studio/studio-server/cms. **TODO deploy** (NON ancora fatto, e2e non verificato in locale per non disturbare il cms attivo + evitare il push drizzle che si blocca): in dev `PAYLOAD_PUSH=true` per creare `studio_users` poi `tsx scripts/seed-studio-users.mjs` (2 admin alek+r.russo, 3 editor maricelli/ravelli/silvestri); in prod rigenerare `db/schema.sql` + seed + settare `KYRON_ADMIN_EMAILS` su studio; poi svuotare `KYRON_REVIEW_EMAILS`.
- [Studio+server+cms] (Alek) **Feature 008 DEPLOYED in staging** (studio.kyronedu.it). Schema applicato a mano sul Postgres staging cms (`docker exec psql`): `CREATE TABLE studio_users` (role=varchar, lo schema.sql versionato è già stale e Payload non pusha in prod) + seed 5 utenti. Deploy in ordine via Coolify API (l'autodeploy on-push NON scatta su questo stack → trigger manuale): cms `staging` (bc730de) → studio-server `main` (2794ae0) → studio `main` (cbf2da6). Env `KYRON_ADMIN_EMAILS` settato su studio. Verifica e2e prod: `/api/studio-users`→403 (collection live), `/auth/resolve` ritorna admin→admin, editor→editor, sconosciuto→negato; `/login`→200. NOTE: (1) i commit di studio/studio-server includono anche il commit concorrente requestedBy (innocuo, ride-along); (2) `db/schema.sql` NON ancora rigenerato (nuovi ambienti non avranno la tabella finché non lo si rigenera); (3) `KYRON_REVIEW_EMAILS` su studio ora è dead config (il login non la legge più). TODO: revocare token Coolify temporaneo.
- [Studio] (Alek) **Logout in sidebar** (`be2a4c4`, deployato staging). Voce "Esci" nel footer (sopra l'email) → route `/api/logout` (GET/POST) che cancella `kyron-rev` con lo stesso `KYRON_COOKIE_DOMAIN` e redirige a `/login`. INSIGHT: serviva non solo per uscire ma per **rinfrescare il ruolo** — chi era loggato prima del rollout feature-008 aveva un cookie senza `role` → trattato come editor (vedeva solo Tema in Impostazioni); dopo re-login il cookie porta `role: admin` e compaiono Connessioni/Modelli/MCP/Organizzazione. In incognito funzionava subito (cookie nuovo). Verifica prod: `/api/logout`→303 a `/login`.
- [Studio+server+cms] (Alek) Onboarding portale ora traccia **chi** lo richiede: nuovo campo `requestedBy` (email) su `pending-schools`, popolato dall'utente Studio loggato. Catena: route `/agents/onboard-school` ora protetta da `studioAuthMiddleware` (richiede login, 401 senza cookie) → `userEmail` propagato in `runOnboardSchoolAgent` → `writePendingSchoolMarkdown(doc, userEmail)` → campo Payload. Reader `PortalSummary` espone `requestedBy`; UI lo mostra in `PortalsList` (icona User) e nel dettaglio (riga "Richiesto da", card INFORMAZIONI). Mail Resend (`notifyPortalCreated`, cms-032) arricchita con `requestedBy` in subject + body. INSIGHT: l'infra mail esisteva già (hook afterChange create), serviva solo l'identità; l'utente Studio è noto solo via email (no nome/ruolo). Typecheck pulito su studio/studio-server/cms. **DEPLOYED in prod (verificato e2e dall'utente)**: commit scoped per-repo (studio `88e7c1a`, studio-server `7cb0127`, cms `2f90fa4`); i push su `main` hanno fatto ride-along con i commit feature-008 concorrenti dell'altra sessione (stessi repo locali) — innocuo, feature-008 era comunque destinata alla prod. Colonna `pending_schools.requested_by` (varchar) aggiunta A MANO sul Postgres della cms live via `docker exec psql` (`ALTER TABLE ... ADD COLUMN IF NOT EXISTS`), perché `apply-schema.sh` è no-op su DB già popolato e Payload non pusha in prod. TOPOLOGIA scoperta: la cms che serve l'onboarding prod è l'app Coolify **`kyron-staging`** (branch `staging`, UUID `ol1grk0...`), il suo Postgres è `postgres-ol1grk0...`; l'app `kyron:main` è `exited:unhealthy` (inutilizzata). Verifica: `POST /agents/onboard-school` senza cookie → **401** (auth live); onboarding reale → agente in card/lista/dettaglio + riga "Richiesto da" nella mail. `db/schema.sql` aggiornato (colonna versionata per ambienti nuovi). NB: i 2 portali pre-esistenti hanno `requested_by=NULL` (precedono la feature).
