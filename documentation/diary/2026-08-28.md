---
type: diary
project: studio
date: 2026-08-28
---
- [06:10] (Alek) Pannello Catalogo: se `list_products` porta un `channelSlug`, i prezzi in lista sono di quel portale (badge "prezzi su …") e non del `default-channel`. Layout invariato: chat a sinistra, catalogo a destra.
- [06:15] (Alek) `+` sulla lista Catalogo apre il wizard Danea (stesso piano/foto/portali della card in chat). Drawer: Aggiungi foto multipart.
- [10:21] (Alek) Card import: nome precompilato da Description Danea, non dallo SKU.
- [15:50] (Alek) Ordini: filtri e ricerca passano al server. Nuovo motore di query generico in studio-server (`core/query/spec.ts`: condizioni JSON validate zod, `all`=AND / `any`=OR / `sort`) + mappa campi ordini (`features/orders/query-fields.ts`, 24 campi tra cui `totale`, `prodotti`, `metodoPagamento`). Nico compone la query da solo: `list_orders` ora prende `spec` e non piu' i quattro filtri fissi. Insight: `statusBucketOf` esisteva in tre copie (route, tool, pannello) e quando divergevano il conteggio in chat non tornava coi KPI in pagina — ora ne resta una sola, lato server.
- [15:52] (Alek) Ordini: tutti i filtri vivono nell'URL, la pagina non filtra piu' niente in memoria — un filtro complesso e' un link condivisibile e il tasto indietro funziona. Ricerca con debounce 300ms. Cache di processo 60s su `fetchOrdersForRange` (TTL, invalidata dalle scritture): senza, ogni tocco di filtro riscaricava ~500 ordini da Saleor.
- [15:54] (Alek) Ordini: i cinque widget in testata sono le `StatTile` della dashboard in taglia `sm` (nuove prop `size`/`active`/`onClick`, utili anche altrove). Confermati/Da confermare/Annullati restano cliccabili come filtro stato; i numeri arrivano da `buckets` calcolati dal server, quindi coincidono con quelli che Nico dice in chat.
- [16:05] (Alek) Fix locale: Impostazioni → Provider AI dava "Server 401" e sembrava chiave persa. Non era la chiave (test provider ok, 135 modelli): era il cookie `kyron-rev` nel browser scaduto, e il middleware dev lo rifirmava SOLO se assente. Ora verifica firma + scadenza e rifirma anche quando c'e' ma non vale.
- [16:30] (Alek) Nico Ordini, due bug dalla stessa sessione. (1) `list_orders` andava in errore di validazione: il modello scriveva `{"portaleNome":"eq","value":"..."}` invece di `{"field","op","value"}`. Colpa degli esempi nella description del tool, scritti in stenografia posizionale `{totale,gte,600}` — non JSON valido e senza i nomi delle chiavi: il modello copiava la forma, non lo schema. Ora gli esempi sono JSON vero. (2) "solo portale pbs" tornava 0 ordini: `portale` e' lo slug tecnico del canale e Nico lo indovinava. Ora ogni risposta di `list_orders` porta `portaliDisponibili`/`agentiDisponibili` del periodo intero, e la description dice di usare `portaleNome contains` quando la scuola viene nominata a parole.
