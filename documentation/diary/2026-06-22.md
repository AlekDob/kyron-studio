---
type: diary
project: kyron-studio
date: 2026-06-22
---
- [16:30] (Alek) Duplica portale da Studio (richiesta Roberto): bottone Copy su ogni card → DuplicatePortalModal (nome + slug con slugify live) → crea una Bozza clonando catalogo/tagli/sconti/bundle+prezzi/spedizione, resettando identita' scuola (slug+nome nuovi, indirizzo svuotato, cod. meccanografico→TBD, sito→vuoto, logo da ricaricare, status=draft). channelId/voucher NON copiati: la copia non tocca Saleor/Stripe, le risorse si generano solo all'Abilita (coerente col gotcha channelId diverge staging/prod). Backend `duplicatePortal()` in studio-server/portals/writer.ts (valida slug kebab-case + univoco, legge sorgente via getPortal, gateway.create) + route `POST /api/v1/portals/:slug/duplicate` (requestedBy = studioUser loggato). Proxy `POST /api/portals/[slug]/duplicate` + client gateway. Flusso: dopo la copia si apre il dettaglio della nuova Bozza per gli aggiustamenti inline. Slug gia' esistente → 400, modal resta aperto col messaggio. Typecheck studio + studio-server puliti; smoke test runtime NON eseguito (server dev non attivi). Feature 007 aggiornata.
- [17:50] (Alek) Go-live Duplica portale: la feature era ferma nel working tree (mai committata/deployata). Commit + push su main (studio `feat(portals): duplica portale (modal + BFF proxy)`, studio-server `feat(portals): endpoint POST /:slug/duplicate + duplicatePortal()` + commit docs separato). Redeploy Coolify manuale via API (SSH localhost:8000): studio `qp5tw0o90drest5wnnof8647` + studio-server `x5bzjhuxbl4ab4j5tnkbckq0`, entrambi `finished`. Health: studio 307 (login gate), studio-server 404 root (atteso Hono). Funzione confermata live (screenshot bottone Copy su tutte le card). Annuncio interno al team via mail "da Panzerottino" (Resend, id `8e6d1404...`) con screenshot inline cid → team@kyronedu.it + cc Alek; artefatti in `documentation/emails/2026-06-22-duplica-portale-panzerottino/`. NB: refuso primo invio a `tea@` (bounce), reinviato a `team@`.
