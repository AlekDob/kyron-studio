---
type: diary
project: kyron-studio
date: 2026-08-31
---
- [~] (Alek) Dark mode per lo Studio (feature 020): blocco token `html[data-theme="dark"]` in globals.css con la stessa strategia della feature 014 (cambiare i valori, non i nomi — ~1.300 className si sono ridisegnati da soli). Scrivania quasi nera con sfere indaco piu' accese, lastra grafite (override del bianco hardcodato nel `::before` di studio-core), card `#232429`, accent alzato a `#7b84f3`. Tab "Tema" tornata nei settings (Chiaro/Scuro/Sistema, per tutti, default per gli editor), chiave `kyron-studio-theme` in localStorage + script inline anti-flash in layout.tsx (`suppressHydrationWarning` su `<html>` obbligatorio: lo script cambia l'attributo prima dell'hydration). Gradienti delle StatTile usciti dal TSX verso token `--tile-*` cosi' il dark li ribalta in CSS. KEY INSIGHT: il vetro di studio-core non e' tutto a token — il `::before` della lastra ha il bianco hardcodato e va sovrascritto a mano; e le utility `dark:` di shadcn vanno agganciate a `data-theme` con `@custom-variant`, altrimenti seguono il sistema e non la scelta utente.
- [~] (Alek) Dark mode promosso in studio-core 0.4.0 (pubblicato su GitHub Packages): blocco `html[data-theme="dark"]` dentro theme.css e vetro della lastra tokenizzato (`--studio-content-glass` — era l'unico rgb() hardcodato, in Kyron lo sovrascrivevamo dall'esterno). Lo Studio Kyron ora consuma ^0.4.0 e in globals.css restano solo i pezzi app-specific (ring mobile, tile cruscotto, @custom-variant per shadcn). README del core con la sezione "Dark mode" (contratto data-theme, script anti-flash, rebind dark:). KEY INSIGHT: la guardia `check-no-client-names` del core blocca anche i nomi cliente nei COMMENTI CSS; e il commit va fatto su main, non sul feature branch WIP che era checked-out nel repo.
- [~] (Alek) Fix dark per le card agenti del cruscotto (e tutte le GlassCard): il vetro era hardcodato `bg-white/45 border-white/60` dentro studio-core, in dark restavano lastre chiare. Ricetta tokenizzata in `--studio-card-glass{,-hover,-line}` (light identico a prima, dark = velo chiaro al 7% cosi' la card resta piu' chiara della lastra) → studio-core 0.4.1 pubblicato, bump qui. KEY INSIGHT: leggere `backgroundColor` con getComputedStyle subito dopo un cambio tema mente se c'e' `transition-colors` — restituisce il frame iniziale della transizione; leggere la custom property, che non transisce.
- [~] (Alek) Dashboard a tutta larghezza come Ordini: via il `max-w-6xl` dalla home, restano i padding.
- [11:15] (Alek) Ordini spostato in cima alla sidebar (seconda voce, sotto la Dashboard) con la nota "Nico" accanto. Serviti due innesti nel core (studio-core 0.4.2): `meta` su ModuleDefinition e i `pinned` renderizzati sotto la Dashboard invece che sopra.
- [~] (Alek) Nuova sezione **Prodotti** con Teo: `/catalogo` -> `/prodotti` (308), agente nuovo (`agentId: "products"`, faccia nuova) e UI gemella di Ordini — filtri nell'URL (`q`/`cat`/`portale`/`stato`/`ord`), 4 tile in testata (le due di stato filtrano), frase-filtro coi chip, lista raggruppata per categoria, scheda a 2 tab (Informazioni · Prezzi e portali) che prende il posto della lista su desktop e diventa bottom sheet sotto i 1024px. Nico resta su Ordini (ordini + DDT), tutti i tool catalogo sono di Teo. Lato server nessun refactor: lo `scope` sul filo resta `"catalogo"` (e' il ramo prompt/tool, non il nome dell'agente), cambiati solo i prompt + `_ui` `ProductsReceipt` su `list_products`/`get_product` e il parametro `tab`. Cancellati `CatalogoWorkspace`, `ProductsPanel`, `ProductRow`, `ProductDrawer` e il CSS `.catalog-glass`; il `Chip`/`Options` della frase e' ora condiviso in `orders/sentence-chips.tsx`. KEY INSIGHT: i filtri girano nella page e non nel BFF perche' `listProducts()` torna gia' tutto il catalogo (<=500 prodotti) — e nella ricevuta in chat NON va il conteggio, perche' il tool cerca per sottostringa mentre il pannello cerca in fuzzy e i due numeri si smentirebbero a schermo.
- [16:40] (Alek) Rifiniture Prodotti/Teo dopo il primo giro a mano. UI: tab **Varianti** a se' stante, gallery completa in Informazioni (`media { url(size:256) }`) + foto per variante (`url(size:128)`, max 3 per riga) — e se le varianti sono piu' di una la griglia del prodotto sparisce, con un rimando al tab Varianti (su Saleor ogni prodotto ha almeno UNA variante: la prima versione nascondeva le foto anche alle punte Apple Pencil). Nuova sezione **Sconti per portale** nel tab prezzi: il channel listing tiene sempre il listino (509 su tutti i 42 portali), lo sconto vero sta nelle promotion CATALOGUE, quindi `listProductDiscounts` chiede a Saleor il `pricing` risolto con un alias per canale (blocchi da 20) invece di combinare le regole a mano. Fixato il modale ImportWizard che si apriva dietro la pagina (z-index: 70/80, la scala del Drawer del core). Locale: aggiunte `SALEOR_ADMIN_EMAIL/PASSWORD` prod all'env di studio-server (le letture catalogo passano dal login admin, il token app non basta). Agente: `get_product` cerca per nome se lo slug non esiste (piu' hit -> elenca gli slug) e due regole di prompt — cercare e' una CHIAMATA anche se il nome e' nel contesto UI, e il canale si chiede solo per le scritture. KEY INSIGHT: due bug che sembravano di pannello erano di prompt — Teo rispondeva "ho trovato..." leggendo i nomi dal contesto UI senza chiamare `list_products` (lista ferma), e su "trova pencil" chiedeva il canale per una lettura. E gli slug sono i codici Danea (`mx763zm-a`): l'agente non puo' indovinarli, il fallback per nome e' l'unica strada.
- [~] (Alek) **Portali rifatto con la UX di Ordini e Prodotti**, e spostato in sidebar sotto Prodotti (quarta voce, `pinned` + meta "Livia"). Prima era il modulo pre-pattern: chat a sinistra, pannellino a destra, tutto lo stato in `useState`, nessun filtro nell'URL. Ora: filtri in URL (`q`/`stato`/`citta'`/`ord`, `agente=1` quando li scrive Livia) applicati nella page (`listPortals()` torna gia' tutto), 4 tile in testata (Portali · Live · Bozze · Kit, le due di stato filtrano col secondo click che azzera), frase-filtro coi chip condivisi di `orders/sentence-chips`, lista raggruppata Bozze/Live senza piu' azioni sulla riga, scheda a 3 tab (Informazioni · Catalogo · Kit) che prende il posto della lista su desktop e diventa bottom sheet sotto i 1024px, con stato/duplica/elimina + Abilita fissi sopra i tab. Lato server `_ui: PortalsReceipt` su `list_portals` (che ora accetta anche `search`/`status`/`city`) e su `get_portal` (con `tab`), piu' `portals-panel-context` per riapplicare una ricevuta vecchia. La card "nuovo portale" di Livia resta **dentro la colonna chat**: il pannello non si muove finche' il portale non e' salvato. Cancellato `PortalsDashboard.tsx` (codice morto). KEY INSIGHT: la riga della lista porta solo il riassunto (niente catalogo ne' kit), quindi la scheda va letta dal BFF per slug — a differenza di Prodotti serve un wrapper che fa il fetch e mostra lo skeleton della scheda; e `PortalCatalogSections` non e' stato spezzato in due file ma ha preso un `only="products"|"kit"`, cosi' il drawer read-only dentro Prodotti resta identico.
- [~] (Alek) Nuova sezione **Clienti** con **Bea** (feature 021). Il cliente non e' una collection: e' chi ha ordinato, derivato dagli ordini Saleor gia' in cache e raggruppato per email (identita' = email, finestra 365gg, funziona anche sui checkout senza account). Backend `studio-server/src/features/customers/`: `derive.ts` puro + self-check, `CUSTOMER_FIELDS` come seconda `FieldMap` del motore query generico (era previsto nella feature 010), `/api/v1/customers` lista + `/:email` scheda con ordini, comunicazioni (Resend + email-log) e nota. Frontend `/clienti` gemello di Ordini/Prodotti — filtri nell'URL, 4 tile (Nuovi e Ricorrenti filtrano), frase coi chip, scheda a 4 tab (Anagrafica · Ordini · Comunicazioni · Note), zero primitive nuove: `sentence-chips`, `orders-period`, `detail-section`, `OrderListRow`, `StatTile` tutti riusati. Lista piatta di proposito: un cliente non e' un evento datato. KEY INSIGHT: i KPI si contano PRIMA del filtro gruppo, altrimenti cliccando "Ricorrenti" spariscono i nuovi dalla tile; e un cliente puo' essere sia nuovo che ricorrente, quindi le due tile non sommano.
- [~] (Alek) Il motore mail di massa e' ora **condiviso** tra Nico e Bea: lotti da 50, claim-before-send su Payload, allowlist e kill switch sfilati dal file DDT verso `core/email/campaign.ts` + `campaign-template.ts`. Cambia solo chi risolve i destinatari — Nico dall'export Danea, Bea dalla lista clienti filtrata o da un segmento salvato. Stesse env `DDT_MAIL_ENABLED`/`DDT_MAIL_ALLOW`: un solo interruttore per fermare tutto. Niente factory `campaignMailTools`: i nomi dei tool di Nico non si toccano, Bea ha i suoi `*_customer_*`. La sua card non ha il bottone "Invia prova" (vuole un importId), la prova si chiede in chat con `send_customer_test_mail`.
- [~] (Alek) Note e segmenti clienti su Payload: due collection NUOVE (`customer-notes` email unique, `customer-segments` slug unique + spec JSON) + migration idempotente `cms/db/migrations/0004-customers.sql`, applicata al boot da `apply-schema.sh`. Tabelle nuove e non colonne su tabelle esistenti, cosi' se la migration non gira nessun endpoint esistente va in 500 (lezione del 2026-07-22). La nota si accoda e non si sovrascrive, come `add_order_note`: la scrivono sia i colleghi che Bea (`add_customer_note`, `save_segment`, `list_segments`). **Serve un redeploy del CMS** perche' le tabelle esistano in prod. Fix collaterale in `lib/query-spec.ts`: `specChips` leggeva `all`/`any` come se fossero sempre presenti, ma una spec puo' arrivare da un URL scritto a mano — guardia messa nella funzione condivisa, quindi vale anche per Ordini.
